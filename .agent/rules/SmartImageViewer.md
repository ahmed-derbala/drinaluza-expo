---
trigger: always_on
---

## Image Component Restrictions
You must **NEVER** use standard React Native `<Image>`, Expo `<Image>`, or HTML `<img>` tags directly for displaying images. 

Always use the custom `SmartImageViewer` component for all image rendering across both Web and Mobile platforms.

