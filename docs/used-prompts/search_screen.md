create search screen 
- use expo-router best practices
- use SmartHeader
- show a search bar with a button to empty the search bar
- wait one second after the user stops typing to launch a search request
- store search queries using storage
- a button to show search queries history
- use GET /api/search?q=shrimp&scopes=products,users&page=1&limit=10
- a filter section that can be hidden, for now it supports only scopes, i will add price range and other filters later
